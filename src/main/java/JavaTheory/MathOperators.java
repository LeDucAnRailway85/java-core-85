package JavaTheory;

public class MathOperators {
    /*
         - Các toán tử trong Java:
            Addition (+)
            subtraction(-)
            multiplication(*)
            division(/)
            Remainder(%)



          - Basic Math Functions:

            Math.abs()      -> trị tuyệt đối
            Math.ceil()     -> Làm tròn lên
            Math.floor()    -> Làm tròn xuống
            Math.floorDiv()
            Math.min()ax    -> Giá trị nhỏ nhất của 2 int
            Math.max()      -> Giá trị lớn nhất của 2 int
            Math.round()    -> Làm tròn thành số nguyên gần nhất
            Math.random()   -> Random ngẫu nhiên 1 số

                Ex: Random int number trong khoảng được chỉ định.
                    Random generator = new Random();
                    int value = generator.nextInt((max - min) + 1) + min;

           - Sử dụng java.util.UUID cũng là 1 cách để random .

                    UUID (Universally Unique Identifier) hay còn được biết đến như là GUID (Globally Unique Identifier),
                        là một chuỗi đại diện cho một mã định danh duy nhất, không thể thay đổi (immutable), và có giá trị dài 128 bit.

                    UUID được sử dụng để tạo tên file ngẫu nhiên, id session trong ứng dụng web, id transaction,..

                    Phương thức java.util.UUID.randomUUID() được sử dụng để sinh ra một chuỗi ngẫu nhiên có độ dài 32 ký tự (128 bit)
                        được biểu diễn ở hệ hệ thập lục phân (hex: 0-9A-F) và 4 ký tự phân tách (–).








     */
}
